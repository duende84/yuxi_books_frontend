<h2>All Books</h2>
<ul>
  {{#each model as |book|}}
    <li>
      {{#link-to 'book' book}}
        {{book.title}} - {{book.author}}
      {{/link-to}}
    </li>
  {{else}}
    <li>No books found.</li>
  {{/each}}
</ul>
